//Testing the Quick BDD framework

import Quick
import Nimble

class searchTest: QuickSpec {
    
    override func spec() {
        it("formats the search term properly") {
            expect(true).to.beTrue()
        }
    }
}